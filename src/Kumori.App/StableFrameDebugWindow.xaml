<Window x:Class="Kumori.App.StableFrameDebugWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Kumori Stable Frame Debug"
        Width="680" Height="620"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource Brush.BackgroundPrimary}">
    <DockPanel Margin="16">
        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Arm Memory Snapshot" Click="ArmSnapshot_Click" Margin="0,0,8,0" />
            <Button Content="Refresh" Click="Refresh_Click" Margin="0,0,8,0" />
            <Button Content="Copy" Click="Copy_Click" Margin="0,0,8,0" />
            <Button Content="Close" Click="Close_Click" />
        </StackPanel>
        <TextBox x:Name="StatusText"
                 IsReadOnly="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 Background="{StaticResource Brush.BackgroundSecondary}"
                 Foreground="{StaticResource Brush.TextPrimary}"
                 BorderBrush="{StaticResource Brush.Border}"
                 FontFamily="Consolas"
                 FontSize="12" />
    </DockPanel>
</Window>

<Window x:Class="Kumori.App.UpdateCheckWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Update Check"
        Width="560" Height="340"
        MinWidth="420" MinHeight="280"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource Brush.BackgroundPrimary}">
    <DockPanel Margin="16">
        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Check" Click="Check_Click" Margin="0,0,8,0" />
            <Button Content="Open releases" Click="Open_Click" Margin="0,0,8,0" />
            <Button Content="Close" Click="Close_Click" />
        </StackPanel>
        <TextBox x:Name="StatusText"
                 IsReadOnly="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 FontFamily="Consolas"
                 FontSize="12"
                 Background="{DynamicResource Brush.BackgroundSecondary}"
                 Foreground="{DynamicResource Brush.TextPrimary}"
                 BorderBrush="{DynamicResource Brush.Border}" />
    </DockPanel>
</Window>

<Window x:Class="Kumori.App.AnalyticsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Kumori Analytics"
        Width="640" Height="560"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource Brush.BackgroundPrimary}">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock x:Name="SummaryText" Text="Loading..." FontSize="16" Margin="0,0,0,12" TextWrapping="Wrap" />
        <ListView Grid.Row="1" x:Name="DailyList"
                  Background="{StaticResource Brush.BackgroundSecondary}"
                  BorderBrush="{StaticResource Brush.BorderSubtle}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Day" DisplayMemberBinding="{Binding Day}" Width="110" />
                    <GridViewColumn Header="Attempts" DisplayMemberBinding="{Binding Attempts}" Width="90" />
                    <GridViewColumn Header="Completed" DisplayMemberBinding="{Binding Completed}" Width="90" />
                    <GridViewColumn Header="Avg Acc" DisplayMemberBinding="{Binding AverageAccuracy}" Width="110" />
                    <GridViewColumn Header="Best PP" DisplayMemberBinding="{Binding BestPp}" Width="90" />
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>

<Window x:Class="Kumori.App.HealthDashboardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Kumori Health"
        Width="640" Height="500"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource Brush.BackgroundPrimary}">
    <DockPanel Margin="16">
        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Refresh" Click="Refresh_Click" Margin="0,0,8,0" />
            <Button Content="Open logs" Click="Logs_Click" Margin="0,0,8,0" />
            <Button Content="Close" Click="Close_Click" />
        </StackPanel>
        <ListView x:Name="HealthList"
                  Background="{StaticResource Brush.BackgroundSecondary}"
                  BorderBrush="{StaticResource Brush.Border}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Component" Width="150" DisplayMemberBinding="{Binding Component}" />
                    <GridViewColumn Header="State" Width="120" DisplayMemberBinding="{Binding State}" />
                    <GridViewColumn Header="Detail" Width="430" DisplayMemberBinding="{Binding Detail}" />
                </GridView>
            </ListView.View>
        </ListView>
    </DockPanel>
</Window>

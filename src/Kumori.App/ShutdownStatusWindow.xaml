<Window x:Class="Kumori.App.ShutdownStatusWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Kumori is exiting"
        Width="414" Height="166"
        SizeToContent="Manual"
        WindowStyle="None"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        ShowActivated="False"
        Focusable="False"
        WindowStartupLocation="Manual"
        Topmost="True"
        Background="Transparent"
        AllowsTransparency="True">
    <Border Background="{DynamicResource Brush.PanelBackground}"
            BorderBrush="{DynamicResource Brush.AccentPink}"
            BorderThickness="1"
            CornerRadius="{DynamicResource Radius.ThemeCard}"
            Margin="12"
            Padding="20">
        <Border.Effect>
            <DropShadowEffect BlurRadius="18"
                              ShadowDepth="4"
                              Opacity="0.45" />
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <TextBlock Text="Kumori is exiting…"
                       FontSize="17"
                       FontWeight="SemiBold"
                       Foreground="{DynamicResource Brush.TextPrimary}" />
            <TextBlock x:Name="StatusMessage" Grid.Row="1"
                       Text="Finishing capture and closing companion services."
                       Margin="0,7,0,14"
                       FontSize="11"
                       Foreground="{DynamicResource Brush.TextSecondary}" />
            <ProgressBar Grid.Row="2"
                         Height="4"
                         IsIndeterminate="True"
                         Foreground="{DynamicResource Brush.AccentPink}"
                         Background="{DynamicResource Brush.ControlBackground}"
                         BorderThickness="0" />
        </Grid>
    </Border>
</Window>
